node_modules/
dist/
coverage/
.e2e-types/
*.tsbuildinfo
pnpm-lock.yaml
.claude/worktrees/

# Pipeline evidence is emitted through canonicalJson: recursively sorted keys,
# two-space indentation and stable multiline arrays are part of its byte-level
# reproducibility contract rather than authored JSON style.
assets/aircraft/candidates/**/*.json

# Authored prose — keep the original formatting so diffs against the source
# document stay readable.
docs/tailfin-design-doc.md

# Licence text, verbatim. The AGPL-3.0 must stay byte-identical to the FSF's
# canonical copy — reformatting it would both misstate the licence and break
# GitHub's licence detection, which matches against the exact text.
LICENSE
docs/LICENSE
